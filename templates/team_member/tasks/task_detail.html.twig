{% extends 'team_member/base_team.html.twig' %}

{% block title %}Détail: {{ task.nom }}{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    
    {# Breadcrumb #}
    <nav class="flex mb-6 text-gray-500 text-sm" aria-label="Breadcrumb">
        <ol class="inline-flex items-center space-x-1 md:space-x-3">
            <li class="inline-flex items-center">
                <a href="{{ path('team_dashboard') }}" class="inline-flex items-center hover:text-gray-900 dark:hover:text-white">
                    <i class="fas fa-home mr-2"></i>Dashboard
                </a>
            </li>
            <li>
                <div class="flex items-center">
                    <i class="fas fa-chevron-right mx-2"></i>
                    <a href="{{ path('team_tasks') }}" class="hover:text-gray-900 dark:hover:text-white">Mes Tâches</a>
                </div>
            </li>
            <li aria-current="page">
                <div class="flex items-center">
                    <i class="fas fa-chevron-right mx-2"></i>
                    <span class="text-gray-400">{{ task.nom|u.truncate(30, '...') }}</span>
                </div>
            </li>
        </ol>
    </nav>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        {# Colonne Gauche : Détails Tâche (2/3) #}
        <div class="lg:col-span-2 space-y-6">
            
            {# Header Tâche #}
            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-100 dark:border-gray-700 p-6">
                <div class="flex flex-wrap items-start justify-between gap-4 mb-4">
                    <div>
                        <div class="flex items-center gap-3 mb-2">
                            {# Badge Priorité #}
                            {% if task.priorite == 'URGENT' %}
                                <span class="px-3 py-1 bg-red-100 text-red-800 text-xs rounded-full font-medium">🔴 URGENT</span>
                            {% elseif task.priorite == 'ELEVE' %}
                                <span class="px-3 py-1 bg-orange-100 text-orange-800 text-xs rounded-full font-medium">🟡 ÉLEVÉ</span>
                            {% elseif task.priorite == 'MOYEN' %}
                                <span class="px-3 py-1 bg-yellow-100 text-yellow-800 text-xs rounded-full font-medium">⚪ MOYEN</span>
                            {% else %}
                                <span class="px-3 py-1 bg-gray-100 text-gray-800 text-xs rounded-full font-medium">⚫ FAIBLE</span>
                            {% endif %}

                            {# Badge Statut #}
                            {% if task.statut == 'TERMINE' %}
                                <span class="px-3 py-1 bg-green-100 text-green-800 text-xs rounded-full font-medium">
                                    <i class="fas fa-check-circle mr-1"></i>Terminée
                                </span>
                            {% elseif task.statut == 'EN_COURS' %}
                                <span class="px-3 py-1 bg-blue-100 text-blue-800 text-xs rounded-full font-medium">
                                    <i class="fas fa-spinner mr-1"></i>En cours
                                </span>
                            {% else %}
                                <span class="px-3 py-1 bg-gray-100 text-gray-800 text-xs rounded-full font-medium">
                                    <i class="fas fa-circle mr-1"></i>À faire
                                </span>
                            {% endif %}
                        </div>
                        
                        <h1 class="text-3xl font-bold text-gray-800 dark:text-white mb-2">{{ task.nom }}</h1>
                        
                        <div class="flex flex-wrap gap-4 text-sm text-gray-600 dark:text-gray-400">
                            {% if task.phase and task.phase.projet %}
                                <span class="flex items-center">
                                    <i class="fas fa-folder text-indigo-500 mr-2"></i>
                                    {{ task.phase.projet.nom }}
                                </span>
                                <span class="flex items-center">
                                    <i class="fas fa-layer-group text-purple-500 mr-2"></i>
                                    {{ task.phase.nom }}
                                </span>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <div class="prose dark:prose-invert max-w-none mt-6 pt-6 border-t border-gray-100 dark:border-gray-700">
                    <h3 class="text-lg font-semibold text-gray-800 dark:text-white mb-2">Description</h3>
                    <p class="text-gray-600 dark:text-gray-300 whitespace-pre-line">
                        {{ task.description|default('Aucune description fournie.') }}
                    </p>
                </div>
            </div>

            {# Historique du Temps #}
            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-100 dark:border-gray-700 p-6">
                <h3 class="text-lg font-semibold text-gray-800 dark:text-white mb-4 flex items-center gap-2">
                    <i class="fas fa-history text-gray-500"></i>
                    Historique des sessions
                </h3>
                
                {% if timeSessions|length > 0 %}
                    <div class="space-y-3">
                        {% for session in timeSessions %}
                            <div class="flex justify-between items-center p-3 bg-gray-50 dark:bg-gray-700/50 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors">
                                <div class="flex items-center gap-3">
                                    <div class="w-8 h-8 rounded-full bg-blue-100 dark:bg-blue-900 flex items-center justify-center text-blue-600 dark:text-blue-300">
                                        <i class="fas fa-clock text-sm"></i>
                                    </div>
                                    <div>
                                        <p class="text-sm font-medium text-gray-800 dark:text-white">
                                            {{ session.dateDebut|date('d/m/Y') }}
                                        </p>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">
                                            {{ session.dateDebut|date('H:i') }} - 
                                            {% if session.dateFin %}
                                                {{ session.dateFin|date('H:i') }}
                                            {% else %}
                                                <span class="text-green-600 font-medium">En cours...</span>
                                            {% endif %}
                                        </p>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <span class="font-mono font-semibold text-indigo-600 dark:text-indigo-400">
                                        {{ session.dureeFormatee }}
                                    </span>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="text-center py-8 text-gray-500 dark:text-gray-400">
                        <i class="fas fa-stopwatch text-3xl mb-2 opacity-50"></i>
                        <p>Aucune session de travail enregistrée</p>
                    </div>
                {% endif %}
            </div>

        </div>

        {# Colonne Droite : Timer & Actions (1/3) #}
        <div class="space-y-6">
            
            {# Timer Widget #}
            <div class="bg-gradient-to-br from-indigo-600 to-purple-700 rounded-xl shadow-lg p-6 text-white relative overflow-hidden">
                <div class="absolute top-0 right-0 -mt-4 -mr-4 w-24 h-24 bg-white opacity-10 rounded-full blur-xl"></div>
                <div class="absolute bottom-0 left-0 -mb-4 -ml-4 w-24 h-24 bg-black opacity-10 rounded-full blur-xl"></div>
                
                <h3 class="text-indigo-100 font-medium mb-4 flex items-center gap-2">
                    <i class="fas fa-stopwatch text-orange-500"></i> Timer
                </h3>

                <div class="text-center mb-6">
                    <div id="timerDisplay" class="text-5xl font-mono text-black font-bold tracking-wider mb-2">
                        00:00:00
                    </div>
                    <p class="text-indigo-200 text-sm">
                        Total cumulé : <span id="totalTimeDisplay">{{ totalTime }}</span>
                    </p>
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <button id="startBtn" onclick="startTimer()" class="py-3 px-4 bg-white text-indigo-600 rounded-lg font-bold hover:bg-indigo-50 transition-colors shadow-md flex items-center justify-center gap-2 {{ hasActiveTimer ? 'hidden' : '' }}">
                        <i class="fas fa-play"></i> Démarrer
                    </button>
                    
                    <button id="stopBtn" onclick="stopTimer()" class="py-3 px-4 bg-red-500 text-white rounded-lg font-bold hover:bg-red-600 transition-colors shadow-md flex items-center justify-center gap-2 {{ not hasActiveTimer ? 'hidden' : '' }} col-span-2">
                        <i class="fas fa-stop"></i> Arrêter
                    </button>
                </div>
                
                {% if hasActiveTimer %}
                <div class="mt-4 flex items-center justify-center gap-2 text-indigo-200 text-sm animate-pulse">
                    <div class="w-2 h-2 bg-green-400 rounded-full"></div>
                    Timer en cours...
                </div>
                {% endif %}
            </div>

            {# Actions #}
            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-100 dark:border-gray-700 p-6">
                <h3 class="font-semibold text-gray-800 dark:text-white mb-4">Actions</h3>
                
                <form method="POST" action="{{ path('team_task_update_status', {id: task.id}) }}">
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Statut</label>
                        <select name="statut" class="w-full px-4 py-2 border dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-100 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500">
                            <option value="A_FAIRE" {{ task.statut == 'A_FAIRE' ? 'selected' : '' }}>À faire</option>
                            <option value="EN_COURS" {{ task.statut == 'EN_COURS' ? 'selected' : '' }}>En cours</option>
                            <option value="TERMINE" {{ task.statut == 'TERMINE' ? 'selected' : '' }}>Terminée</option>
                        </select>
                    </div>
                    <button type="submit" class="w-full py-2 px-4 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors">
                        Mettre à jour le statut
                    </button>
                </form>

                {% if task.statut != 'TERMINE' %}
                    <div class="mt-4 pt-4 border-t border-gray-100 dark:border-gray-700">
                        <form method="POST" action="{{ path('team_task_update_status', {id: task.id}) }}">
                            <input type="hidden" name="statut" value="TERMINE">
                            <button type="submit" class="w-full py-2 px-4 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors flex items-center justify-center gap-2">
                                <i class="fas fa-check"></i> Marquer comme terminée
                            </button>
                        </form>
                    </div>
                {% endif %}
            </div>

            {# Infos Complémentaires #}
            <div class="bg-white dark:bg-gray-800 rounded-lg shadow-sm border border-gray-100 dark:border-gray-700 p-6">
                <h3 class="font-semibold text-gray-800 dark:text-white mb-4">Détails</h3>
                <ul class="space-y-3 text-sm">
                    <li class="flex justify-between">
                        <span class="text-gray-500 dark:text-gray-400">Deadline</span>
                        <span class="font-medium text-gray-800 dark:text-white">{{ task.deadline|date('d/m/Y') }}</span>
                    </li>
                    <li class="flex justify-between">
                        <span class="text-gray-500 dark:text-gray-400">Créée le</span>
                        <span class="font-medium text-gray-800 dark:text-white">{{ task.dateCreation|date('d/m/Y') }}</span>
                    </li>
                    <li class="flex justify-between">
                        <span class="text-gray-500 dark:text-gray-400">Priorité</span>
                        <span class="font-medium text-gray-800 dark:text-white">{{ task.priorite }}</span>
                    </li>
                </ul>
            </div>

        </div>
    </div>
</div>

<script>
    // Variables globales pour le timer
    let timerInterval;
    let startTime;
    const taskId = {{ task.id }};
    const hasActiveTimer = {{ hasActiveTimer ? 'true' : 'false' }};
    
    // Initialisation
    document.addEventListener('DOMContentLoaded', function() {
        if (hasActiveTimer) {
            // Si un timer est déjà actif, on récupère sa date de début
            {% if activeTimer %}
                const startDateStr = "{{ activeTimer.dateDebut|date('Y-m-d H:i:s') }}";
                startTime = new Date(startDateStr);
                startTicker();
            {% endif %}
        }
    });

    function startTimer() {
        fetch(`/team/tasks/${taskId}/timer/start`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            }
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                startTime = new Date(data.start_time);
                startTicker();
                toggleButtons(true);
                // Notification (optionnel)
                console.log('Timer démarré');
            } else {
                alert(data.error || 'Erreur lors du démarrage du timer');
            }
        })
        .catch(error => console.error('Erreur:', error));
    }

    function stopTimer() {
        fetch(`/team/tasks/${taskId}/timer/stop`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            }
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                stopTicker();
                toggleButtons(false);
                document.getElementById('totalTimeDisplay').textContent = data.total_time_formatted;
                document.getElementById('timerDisplay').textContent = "00:00:00";
                // Recharger pour mettre à jour l'historique
                window.location.reload();
            } else {
                alert(data.error || 'Erreur lors de l\'arrêt du timer');
            }
        })
        .catch(error => console.error('Erreur:', error));
    }

    function startTicker() {
        if (timerInterval) clearInterval(timerInterval);
        timerInterval = setInterval(updateDisplay, 1000);
        updateDisplay(); // Mise à jour immédiate
    }

    function stopTicker() {
        if (timerInterval) clearInterval(timerInterval);
    }

    function updateDisplay() {
        const now = new Date();
        // Ajustement du fuseau horaire si nécessaire, mais ici on travaille en local ou UTC selon config
        // Pour simplifier, on suppose que le serveur et le client sont synchro ou on utilise le timestamp
        const diff = Math.floor((now - startTime) / 1000);
        
        if (diff < 0) return; // Éviter affichage négatif si décalage léger

        const hours = Math.floor(diff / 3600);
        const minutes = Math.floor((diff % 3600) / 60);
        const seconds = diff % 60;

        document.getElementById('timerDisplay').textContent = 
            `${pad(hours)}:${pad(minutes)}:${pad(seconds)}`;
    }

    function pad(num) {
        return num.toString().padStart(2, '0');
    }

    function toggleButtons(isRunning) {
        const startBtn = document.getElementById('startBtn');
        const stopBtn = document.getElementById('stopBtn');
        
        if (isRunning) {
            startBtn.classList.add('hidden');
            stopBtn.classList.remove('hidden');
        } else {
            startBtn.classList.remove('hidden');
            stopBtn.classList.add('hidden');
        }
    }
</script>
{% endblock %}
