{# templates/default/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Symfony 360 — Gestion Intelligente de Projets{% endblock %}

{% block body %}
<div class="min-h-screen flex flex-col bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-sm">
        <div class="container mx-auto px-4 py-6 flex justify-between items-center">
            <div class="flex items-center space-x-2">
                <div class="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center">
                    <span class="text-white font-bold text-lg">S360</span>
                </div>
                <h1 class="text-2xl font-bold text-gray-900">Symfony 360</h1>
            </div>
            <div class="flex space-x-4">
                <a href="/signin" class="px-4 py-2 text-gray-700 hover:text-blue-600 font-medium">Se connecter</a>
                <a href="{{ path('app_request_account') }}" class="px-4 py-2 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors">Demander un compte</a>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <main class="flex-grow container mx-auto px-4 py-16">
        <div class="max-w-3xl mx-auto text-center">
            <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 mb-6">
                Centralisez la gestion de votre portefeuille de projets
            </h1>
            <p class="text-lg text-gray-600 mb-10 max-w-2xl mx-auto">
                Réduisez les retards, maîtrisez les coûts et améliorez la collaboration entre chefs de projet, équipes et direction — le tout dans une interface unique, intuitive et sécurisée.
            </p>

            <div class="flex flex-col sm:flex-row justify-center gap-4 mb-16">
                <a href="/signin" class="px-8 py-3 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors shadow-md">Se connecter</a>
                <a href="{{ path('app_request_account') }}" class="px-8 py-3 bg-white text-blue-600 border border-blue-600 font-medium rounded-lg hover:bg-blue-50 transition-colors">Demander un compte</a>
            </div>
        </div>

        <!-- Valeur métier (KPIs) -->
        <div class="mb-20 text-center">
            <h2 class="text-3xl font-bold text-gray-900 mb-2">Des résultats concrets, mesurés</h2>
            <p class="text-gray-600 mb-8">Notre application vous aide à atteindre vos objectifs stratégiques</p>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
                <div class="p-6 bg-white rounded-xl shadow-sm border border-gray-100">
                    <div class="text-4xl font-bold text-blue-600 mb-2">+15%</div>
                    <h3 class="text-lg font-semibold text-gray-900">Taux de livraison à l’heure</h3>
                    <p class="text-gray-600 mt-1">En 6 mois, grâce à une meilleure visibilité sur les retards critiques.</p>
                </div>
                <div class="p-6 bg-white rounded-xl shadow-sm border border-gray-100">
                    <div class="text-4xl font-bold text-green-600 mb-2">-10%</div>
                    <h3 class="text-lg font-semibold text-gray-900">Écart de coût moyen</h3>
                    <p class="text-gray-600 mt-1">Via le suivi précis du temps réel et du budget restant.</p>
                </div>
                <div class="p-6 bg-white rounded-xl shadow-sm border border-gray-100">
                    <div class="text-4xl font-bold text-purple-600 mb-2">90%+</div>
                    <h3 class="text-lg font-semibold text-gray-900">Taux d’adoption</h3>
                    <p class="text-gray-600 mt-1">Interface simplifiée pour les membres d’équipe, sans formation requise.</p>
                </div>
            </div>
        </div>

        <!-- Profils utilisateurs -->
        <div class="max-w-4xl mx-auto text-center mb-20">
            <h2 class="text-3xl font-bold text-gray-900 mb-10">Conçue pour tous les rôles de votre organisation</h2>

            <div class="space-y-12">
                <div class="flex flex-col md:flex-row items-center gap-8 p-6 bg-blue-50 rounded-xl">
                    <div class="flex-shrink-0 w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center">
                        <span class="text-blue-700 font-bold text-xl">PM</span>
                    </div>
                    <div class="text-left">
                        <h3 class="text-xl font-bold text-gray-900">Chefs de projet</h3>
                        <p class="text-gray-600 mt-2">Interface riche pour la planification, l’affectation, le suivi budgétaire et la gestion proactive des risques.</p>
                    </div>
                </div>

                <div class="flex flex-col md:flex-row-reverse items-center gap-8 p-6 bg-gray-50 rounded-xl">
                    <div class="flex-shrink-0 w-16 h-16 bg-gray-200 rounded-full flex items-center justify-center">
                        <span class="text-gray-700 font-bold text-xl">T</span>
                    </div>
                    <div class="text-right md:text-left">
                        <h3 class="text-xl font-bold text-gray-900">Membres d’équipe</h3>
                        <p class="text-gray-600 mt-2">Interface simplifiée pour mettre à jour ses tâches et saisir son temps en quelques clics.</p>
                    </div>
                </div>

                <div class="flex flex-col md:flex-row items-center gap-8 p-6 bg-green-50 rounded-xl">
                    <div class="flex-shrink-0 w-16 h-16 bg-green-100 rounded-full flex items-center justify-center">
                        <span class="text-green-700 font-bold text-xl">📊</span>
                    </div>
                    <div class="text-left">
                        <h3 class="text-xl font-bold text-gray-900">Direction & Sponsors</h3>
                        <p class="text-gray-600 mt-2">Tableaux de bord stratégiques avec la « Météo du Projet » et rapports PDF prêts à présenter.</p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t py-8">
        <div class="container mx-auto px-4 text-center text-gray-500 text-sm">
            <p>Symfony 360 — Solution de gestion de projets entreprise — Sécurisée, évolutive, open mindset.</p>
            <p class="mt-2">© {{ 'now'|date('Y') }} Votre Entreprise. Tous droits réservés.</p>
        </div>
    </footer>
</div>
{% endblock %}